% Load the microtype package for better typography
\usepackage{microtype}

% Set hyphenation penalties
\hyphenpenalty=5000
\exhyphenpenalty=5000

% Increase tolerance to avoid overfull lines
\tolerance=1000
\emergencystretch=3em
